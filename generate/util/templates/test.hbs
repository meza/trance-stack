import { {{camelCase name}} } from './{{camelCase name}}';
import { describe, expect, it } from 'vitest';

describe('Given a {{camelCase name}} util function', () => {
  it('should do something', () => {
    expect({{camelCase name}}()).toBe(undefined)
  })
})
